<?xml version="1.0" encoding="utf-8"?>
<!-- Plain white backdrop behind the GoPreach logo (drawable/ic_launcher_foreground.png),
     matching the logo's own white background so the adaptive icon mask crop (circle/squircle/
     rounded-square, depending on launcher) looks seamless. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" />
</shape>
