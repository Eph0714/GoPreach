<?xml version="1.0" encoding="utf-8"?>
<!-- Plain black backdrop behind the GP monogram badge (drawable/ic_launcher_foreground.png),
     matching the badge's own black background so the adaptive icon mask crop (circle/squircle/
     rounded-square, depending on launcher) looks seamless. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#000000" />
</shape>
